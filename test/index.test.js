// import { useHandler } from "../src"

test("no tests here" /* "useHandler should return the same instance" */, () => {
	// const f = useHandler(x => x)
	// expect(f(2)).toBe(2)
	// expect(f).toBe(useHandler(x => x * 2))
	// expect(f(2)).toBe(4)
	// expect(f).toBe(useHandler(x => x * 3))
	// expect(f(2)).toBe(6)
})
